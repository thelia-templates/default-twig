{% component 'BoCreateDialog' with {
    id: 'tax-rule-create-modal',
    title: 'Create a new tax rule',
    form: create_tax_rule_form,
    formAction: path('admin.configuration.taxes-rules.add'),
    submitLabel: 'Create this tax rule',
    testid: 'tax-rule-create',
    hook: 'tax-rule.create-form',
    hookContext: { location: 'tax_rule_create_form' },
    size: '',
} %}
    {% block fields %}
        {{ form_widget(create_tax_rule_form.locale) }}
        {{ form_row(create_tax_rule_form.title) }}
        {{ form_row(create_tax_rule_form.description) }}
    {% endblock %}
{% endcomponent %}

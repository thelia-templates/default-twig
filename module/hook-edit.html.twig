{% extends '@BackOfficeDefaultTwig/base.html.twig' %}

{% block title %}{{ 'Edit module hook'|trans }}{% endblock %}
{% block body_testid %}module-hook-edit{% endblock %}

{% block breadcrumb %}
    <nav aria-label="breadcrumb" class="mb-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('admin.home') }}">{{ 'Home'|trans }}</a></li>
            <li class="breadcrumb-item"><a href="{{ path('admin.module-hook') }}">{{ 'Module hooks'|trans }}</a></li>
            <li class="breadcrumb-item active" aria-current="page">#{{ module_hook.id }}</li>
        </ol>
    </nav>
{% endblock %}

{% block content %}
    <div class="container-fluid" data-testid="module-hook-edit-page">
        <h1 class="h3 mb-3">{{ 'Edit module hook'|trans }} <small class="text-muted">#{{ module_hook.id }}</small></h1>

        <form action="{{ path('admin.module-hook.save', { module_hook_id: module_hook.id }) }}" method="post" data-testid="module-hook-edit-form"
              data-controller="bo-module-hooks"
              data-bo-module-hooks-classnames-url-value="{{ classnames_url_template }}"
              data-bo-module-hooks-methods-url-value="{{ methods_url_template }}">
            <div class="row">
                <div class="col-md-6 mb-3">
                    <label class="form-label" for="module-hook-edit-module">{{ 'Module'|trans }}</label>
                    <select id="module-hook-edit-module" name="module_id" class="form-select"
                            data-bo-module-hooks-target="createModule"
                            data-action="change->bo-module-hooks#loadClassnames">
                        {% for m in available_modules %}
                            <option value="{{ m.id }}" {% if m.id == module_hook.moduleId %}selected{% endif %}>{{ m.code }}</option>
                        {% endfor %}
                    </select>
                </div>
                <div class="col-md-6 mb-3">
                    <label class="form-label" for="module-hook-edit-hook">{{ 'Hook'|trans }}</label>
                    <select id="module-hook-edit-hook" name="hook_id" class="form-select">
                        {% for h in available_hooks %}
                            <option value="{{ h.id }}" {% if h.id == module_hook.hookId %}selected{% endif %}>{{ h.code }}</option>
                        {% endfor %}
                    </select>
                </div>
                <div class="col-md-6 mb-3">
                    <label class="form-label" for="module-hook-edit-classname">{{ 'Class name'|trans }}</label>
                    <select id="module-hook-edit-classname" name="classname" class="form-select"
                            data-bo-module-hooks-target="createClassname"
                            data-action="change->bo-module-hooks#loadMethods"
                            data-placeholder="{{ 'Select a class…'|trans }}"
                            data-current="{{ module_hook.classname }}">
                        <option value="{{ module_hook.classname }}" selected>{{ module_hook.classname }}</option>
                    </select>
                </div>
                <div class="col-md-6 mb-3">
                    <label class="form-label" for="module-hook-edit-method">{{ 'Method'|trans }}</label>
                    <select id="module-hook-edit-method" name="method" class="form-select"
                            data-bo-module-hooks-target="createMethod"
                            data-placeholder="{{ 'Select a method…'|trans }}"
                            data-current="{{ module_hook.method }}">
                        <option value="{{ module_hook.method }}" selected>{{ module_hook.method }}</option>
                    </select>
                </div>
                <div class="col-md-12 mb-3">
                    <label class="form-label">{{ 'Templates'|trans }}</label>
                    <input type="text" name="templates" value="{{ module_hook.templates }}" class="form-control">
                </div>
                <div class="col-md-12 form-check mb-3">
                    <input type="checkbox" name="active" value="1" {% if module_hook.active %}checked{% endif %} class="form-check-input">
                    <label class="form-check-label">{{ 'Active'|trans }}</label>
                </div>
            </div>
            <button type="submit" class="btn btn-primary" data-testid="module-hook-edit-submit">{{ 'Save'|trans }}</button>
            <a href="{{ path('admin.module-hook') }}" class="btn btn-outline-secondary">{{ 'Back'|trans }}</a>
        </form>
    </div>
{% endblock %}
